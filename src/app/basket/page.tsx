import React from 'react';
import style from "@/styles/basket.module.scss"
const Page = () => {
    return (
        <div className={style.basket}>
            basket
        </div>
    );
};

export default Page;